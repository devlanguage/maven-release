#
# @(#)file      README
# @(#)author    Sun Microsystems, Inc.
# @(#)version   1.15
# @(#)lastedit  04/04/28
#
# Copyright 2004 Sun Microsystems, Inc.  All rights reserved. Use is subject to license terms.


                         Java DMK Proxy MBeans Example
                         =============================


-----------------------------------------------------------------------
1. Overview
-----------------------------------------------------------------------

This example contains the Proxy MBeans for some Java DMK MBeans.
These Proxy MBeans have been generated using the com.sun.jdmk.tools.ProxyGen
tool contained in the Java DMK toolkit (jdmktk.jar).


-----------------------------------------------------------------------
2. File List
-----------------------------------------------------------------------

This example is based on the following source files:

 * CounterMonitorProxy.java, CounterMonitorProxyMBean.java

    - The Proxy MBean and its management interface for the
      javax.management.monitor.CounterMonitor MBean.

 * GaugeMonitorProxy.java, GaugeMonitorProxyMBean.java

    - The Proxy MBean and its management interface for the
      javax.management.monitor.GaugeMonitor MBean.

 * StringMonitorProxy.java, StringMonitorProxyMBean.java

   - The Proxy MBean and its management interface for the
     javax.management.monitor.StringMonitor MBean.

 * TimerProxy.java, TimerProxyMBean.java

   - The Proxy MBean and its management interface for the
     javax.management.timer.Timer MBean.

 * MBeanServerDelegateProxy.java, MBeanServerDelegateProxyMBean.java

    - The Proxy MBean and its management interface for the
      javax.management.MBeanServerDelegate MBean.

 * MLetProxy.java, MLetProxyMBean.java

    - The Proxy MBean and its management interface for the
      javax.management.loading.MLet MBean (m-let service).

 * DiscoveryClientProxy.java, DiscoveryClientProxyMBean.java

    - The Proxy MBean and its management interface for the
      com.sun.jdmk.discovery.DiscoveryClient MBean (discovery service).

 * DiscoveryMonitorProxy.java, DiscoveryMonitorProxyMBean.java

    - The Proxy MBean and its management interface for the
      com.sun.jdmk.discovery.DiscoveryMonitor MBean (discovery service).

 * DiscoveryResponderProxy.java, DiscoveryResponderProxyMBean.java

    - The Proxy MBean and its management interface for the
      com.sun.jdmk.discovery.DiscoveryResponder MBean (discovery service).

 * CascadingAgentProxy.java, CascadingAgentProxyMBean.java

    - The Proxy MBean and its management interface for the
      com.sun.jdmk.cascading.CascadingAgent MBean (cascading service).

 * HttpConnectorServerProxy.java, HttpConnectorServerProxyMBean.java

    - The Proxy MBean and its management interface for the
      com.sun.jdmk.comm.HttpConnectorServer MBean (http connector server).

 * HttpsConnectorServerProxy.java, HttpsConnectorServerProxyMBean.java

    - The Proxy MBean and its management interface for the
      com.sun.jdmk.comm.HttpsConnectorServer MBean (https connector server).

 * RmiConnectorServerProxy.java, RmiConnectorServerProxyMBean.java

    - The Proxy MBean and its management interface for the
      com.sun.jdmk.comm.RmiConnectorServer MBean (rmi connector server).

 * SnmpAdaptorServerProxy.java, SnmpAdaptorServerProxyMBean.java

    - The Proxy MBean and its management interface for the
      com.sun.jdmk.comm.SnmpAdaptorServer MBean (snmp protocol adaptor).

 * RelationServiceProxy.java, RelationServiceProxyMBean.java

    - The Proxy MBean and its management interface for the
      javax.management.relation.RelationService MBean (relation service).

 * RelationSupportProxy.java, RelationSupportProxyMBean.java

    - The Proxy MBean and its management interface for the
      javax.management.relation.RelationSupport MBean
      (to be used to handle simple relations as MBeans).

Note: The generated Proxy MBeans for MBeans that belong to a
      javax.management package have no package name.


-----------------------------------------------------------------------
3. Building and Running the Example
-----------------------------------------------------------------------

To build and run this example, make sure that your PATH environment variable
is correctly set to use the Java 2 SDK, Standard Edition 1.4 or later.

On J2SE 1.4, your CLASSPATH must contain the jar files for the JMX runtime
(jmx.jar), the Java DMK runtime (jdmkrt.jar) and the legacy SNMP runtime
(legacysnmp.jar), as well as the current directory (.).

On J2SE 1.5, your CLASSPATH must contain the jar files for the Java DMK
runtime (jdmkrt.jar) and the legacy SNMP runtime (legacysnmp.jar), as
well as the current directory (.).

These instructions assume the classpath is set in an environment variable,
though it may also be given on the command line with the -classpath option.

Copy the example source files to your working directory and type the following
commands:

    cd <WORKING_DIR>

    javac -d . *.java

You may then try to use the Proxy MBeans in a manager application.
